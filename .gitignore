public/
.hugo_build.lock
.DS_Store
resources/_gen/
_archive/

# Licensed source maps (e.g. LDS Bible Maps used by permission) — do NOT commit/publish raw
_source-maps/
*.backup-*

# Cloudflare wrangler local cache (contains account creds)
.wrangler/

# Claude Code worktrees & session checkpoints — never track these.
# They had been committed as stray gitlinks (submodule refs) with no
# .gitmodules entry, which made every `git clone --recurse-submodules`
# fail — that is what broke Cloudflare Pages builds at the clone step.
.claude/worktrees/
.session-checkpoints/

# HELD BACK BY KYMBER'S DECISION, 2026-08-24 — restore after Friday's videos.
# static/ is served DIRECTLY and is NOT gated by draft:, so pushing this file makes
# cfmcorner.com/content/week36/study-guide.html publicly fetchable — 1,125 lines of a
# week she has said she is still working on. Untracked, NOT deleted: the file stays on
# disk so the staging preview at /weeks/36/ keeps working.
# TO RESTORE: delete these lines, then `git add -f static/content/week36/study-guide.html`
static/content/week36/study-guide.html
